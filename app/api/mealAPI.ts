import axios from "axios";

const API_KEY = process.env.EXPO_PUBLIC_API_KEY as string;

const BASE_URL = process.env.EXPO_PUBLIC_API_URL as string;

export const MealsByMood = async () => {
	let query = "";
};
